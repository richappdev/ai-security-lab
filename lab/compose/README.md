# Compose Profiles

Future Docker Compose overlays can live here.

Examples:

- `docker-compose.app.yml` for the security app.
- `docker-compose.storage.yml` for database and queue services.
- `docker-compose.high-risk.yml` for isolated high-risk lab profiles.
