# Lab

This directory holds lab-specific documentation, future compose profiles, and seed data.

The active compose file currently remains at repository root as `docker-compose.yml` so existing scripts keep working.
