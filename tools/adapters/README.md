# Tool Adapters

Adapters integrate external scanners or security tools.

Adapters must not bypass local safety controls. They should translate agent requests into constrained scanner runs against allowlisted lab targets only.
